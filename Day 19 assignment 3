#include <stdio.h>
void main() {
  int n, rev = 0, r;
  scanf("%d", &n);
  while (n != 0) 
  {
    r = n % 10;
    rev = rev * 10 + r;
    n=n/10;
  }
  printf("Reversed number = %d", rev);
  return 0;
}
Output:
1254
Reverse number=4521
